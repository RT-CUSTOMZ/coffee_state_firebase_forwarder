{
    "title": "Firebase Messaging",
    "type": "object",
    "properties": {
        "timestamp": {
            "desciption": "",
            "type": "string",
            "format": "date-time"
        },
        "type": {
            "type": "string",
            "enum": [
                "coffee_brewing",
                "coffee_ready"
            ]
        }
    },
    "additionalProperties": true,
    "required": [
        "type",
        "timestamp"
    ]
}